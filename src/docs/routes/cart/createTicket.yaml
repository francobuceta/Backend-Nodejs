paths:
  /api/cart/{cid}/purchase:
    get:
      summary: Create Ticket
      tags:
        - Cart
      parameters:
        - name: cid
          in: path
          description: Id Cart
          schema:
            type: string
      responses:
        "200":
          description: Ticket created successfully