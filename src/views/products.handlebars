<h2>Hola {{userName}}!</h2>

<div class="products_container">


{{#each products}}

    <div>
        <h3>{{title}}</h3>
        <p>{{description}}</p>
        <h4>Categoría: {{category}}</h4>
        <h4>$ {{price}}</h4>
        <h5>Cantidad en stock: {{stock}}</h5>
        <button id={{_id}}>Agregar al carrito</button>
    </div>

{{/each}}

</div>